import React from "react";
import { IconProps } from ".";

export const {{pascalCase name}}: React.FC<IconProps> = ({ className }) => {
  return (
    <div className={`flex items-center justify-center ${className ? className : ""}`}>
      
    </div>
  );
};
